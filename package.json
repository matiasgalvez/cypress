{
  "scripts": {
    "open-testui": "cls && npx cypress open",
    "open-byconsole-examples": "cls && npx cypress run --browser chrome --headless --spec \"./cypress/integration/examples/*.spec.js\"",
    "open-byconsole": "cls && npx cypress run --browser chrome --headless --spec \"./cypress/integration/*.spec.js\""
  },
  "dependencies": {
    "cypress": "^7.7.0",
    "cypress-plugin-snapshots": "^1.4.4",
    "cypress-xpath": "^1.6.2"
  },
  "devDependencies": {
    "@cypress/snapshot": "^2.1.7"
  }
}
